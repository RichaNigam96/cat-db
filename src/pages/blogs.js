import React from 'react';

const Blogs = () => {
return (
  <div class="main-body"
  style={{
    justifyContent: 'Left',
    alignItems: 'Right',
    height: '100vh',
    margin: '10px'
  }}
  >
  <h2>CatDB's Blogs</h2>
  <p> Currently all the blogs I have are kinda embarassing. But here's <a name="" href="https://www.instagram.com/felix.on.the.gram/">my cat's ig page</a> (You're welcome). </p>
  </div>
);
};

export default Blogs;
